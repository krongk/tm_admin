---
en:
  active_record:
    models:
      template:
        keystore: Keystore
    attributes:
      template:
        keystore:
          template: Template
          key: Key
          value: Value
    errors:
      keystore: 
  helpers:
    keystore: 
