require 'spec_helper'

# This spec was generated by rspec-rails when you ran the scaffold generator.
# It demonstrates how one might use RSpec to specify the controller code that
# was generated by Rails when you ran the scaffold generator.
#
# It assumes that the implementation code is generated by the rails scaffold
# generator.  If you are using any extension libraries to generate different
# controller code, this generated spec may or may not pass.
#
# It only uses APIs available in rails and/or rspec-rails.  There are a number
# of tools you can use to make these specs even more expressive, but we're
# sticking to rails and rspec-rails APIs to keep things simple and stable.
#
# Compared to earlier versions of this generator, there is very limited use of
# stubs and message expectations in this spec.  Stubs are only used when there
# is no simpler way to get a handle on the object needed for the example.
# Message expectations are only used when there is no simpler way to specify
# that an instance is receiving a specific message.

describe Template::ThemesController do

  # This should return the minimal set of attributes required to create a valid
  # Template::Theme. As you add validations to Template::Theme, be sure to
  # adjust the attributes here as well.
  let(:valid_attributes) { { "template_template" => "" } }

  # This should return the minimal set of values that should be in the session
  # in order to pass any filters (e.g. authentication) defined in
  # Template::ThemesController. Be sure to keep this updated too.
  let(:valid_session) { {} }

  describe "GET index" do
    it "assigns all template_themes as @template_themes" do
      theme = Template::Theme.create! valid_attributes
      get :index, {}, valid_session
      assigns(:template_themes).should eq([theme])
    end
  end

  describe "GET show" do
    it "assigns the requested template_theme as @template_theme" do
      theme = Template::Theme.create! valid_attributes
      get :show, {:id => theme.to_param}, valid_session
      assigns(:template_theme).should eq(theme)
    end
  end

  describe "GET new" do
    it "assigns a new template_theme as @template_theme" do
      get :new, {}, valid_session
      assigns(:template_theme).should be_a_new(Template::Theme)
    end
  end

  describe "GET edit" do
    it "assigns the requested template_theme as @template_theme" do
      theme = Template::Theme.create! valid_attributes
      get :edit, {:id => theme.to_param}, valid_session
      assigns(:template_theme).should eq(theme)
    end
  end

  describe "POST create" do
    describe "with valid params" do
      it "creates a new Template::Theme" do
        expect {
          post :create, {:template_theme => valid_attributes}, valid_session
        }.to change(Template::Theme, :count).by(1)
      end

      it "assigns a newly created template_theme as @template_theme" do
        post :create, {:template_theme => valid_attributes}, valid_session
        assigns(:template_theme).should be_a(Template::Theme)
        assigns(:template_theme).should be_persisted
      end

      it "redirects to the created template_theme" do
        post :create, {:template_theme => valid_attributes}, valid_session
        response.should redirect_to(Template::Theme.last)
      end
    end

    describe "with invalid params" do
      it "assigns a newly created but unsaved template_theme as @template_theme" do
        # Trigger the behavior that occurs when invalid params are submitted
        Template::Theme.any_instance.stub(:save).and_return(false)
        post :create, {:template_theme => { "template_template" => "invalid value" }}, valid_session
        assigns(:template_theme).should be_a_new(Template::Theme)
      end

      it "re-renders the 'new' template" do
        # Trigger the behavior that occurs when invalid params are submitted
        Template::Theme.any_instance.stub(:save).and_return(false)
        post :create, {:template_theme => { "template_template" => "invalid value" }}, valid_session
        response.should render_template("new")
      end
    end
  end

  describe "PUT update" do
    describe "with valid params" do
      it "updates the requested template_theme" do
        theme = Template::Theme.create! valid_attributes
        # Assuming there are no other template_themes in the database, this
        # specifies that the Template::Theme created on the previous line
        # receives the :update_attributes message with whatever params are
        # submitted in the request.
        Template::Theme.any_instance.should_receive(:update).with({ "template_template" => "" })
        put :update, {:id => theme.to_param, :template_theme => { "template_template" => "" }}, valid_session
      end

      it "assigns the requested template_theme as @template_theme" do
        theme = Template::Theme.create! valid_attributes
        put :update, {:id => theme.to_param, :template_theme => valid_attributes}, valid_session
        assigns(:template_theme).should eq(theme)
      end

      it "redirects to the template_theme" do
        theme = Template::Theme.create! valid_attributes
        put :update, {:id => theme.to_param, :template_theme => valid_attributes}, valid_session
        response.should redirect_to(theme)
      end
    end

    describe "with invalid params" do
      it "assigns the template_theme as @template_theme" do
        theme = Template::Theme.create! valid_attributes
        # Trigger the behavior that occurs when invalid params are submitted
        Template::Theme.any_instance.stub(:save).and_return(false)
        put :update, {:id => theme.to_param, :template_theme => { "template_template" => "invalid value" }}, valid_session
        assigns(:template_theme).should eq(theme)
      end

      it "re-renders the 'edit' template" do
        theme = Template::Theme.create! valid_attributes
        # Trigger the behavior that occurs when invalid params are submitted
        Template::Theme.any_instance.stub(:save).and_return(false)
        put :update, {:id => theme.to_param, :template_theme => { "template_template" => "invalid value" }}, valid_session
        response.should render_template("edit")
      end
    end
  end

  describe "DELETE destroy" do
    it "destroys the requested template_theme" do
      theme = Template::Theme.create! valid_attributes
      expect {
        delete :destroy, {:id => theme.to_param}, valid_session
      }.to change(Template::Theme, :count).by(-1)
    end

    it "redirects to the template_themes list" do
      theme = Template::Theme.create! valid_attributes
      delete :destroy, {:id => theme.to_param}, valid_session
      response.should redirect_to(template_themes_url)
    end
  end

end
